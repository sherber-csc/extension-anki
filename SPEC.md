# SPEC.md

## 1. 项目目标

本项目用于提升英文视频/网页阅读中的生词采集与 Anki 制卡效率。

核心目标：

- 浏览器扩展只负责**轻量选词采集**
- 本地后端负责**标准化、词形归并、去重、待处理队列、批量生成正式 Anki 卡片**
- 整体设计必须以**不影响观看体验**为最高优先级
- 正式 Anki 卡片继续沿用现有高质量结构：`SYSTEM_PROMPT` + 自定义 note type 字段 + Anki 模板渲染

一句话原则：

> 扩展负责“拿到词”，后端负责“把词变成卡”。

---

## 2. 范围与非目标

### 2.1 第一版范围

第一版必须支持：

- 普通网页中选中单词后加入待处理队列
- YouTube 中尽量沿用相同方式选词并加入待处理队列
- 扩展向本地后端发送采集请求
- 后端执行标准化、词形归并、去重
- 后端维护待处理队列
- 用户在稍后手动触发批量生成正式卡片
- 生成正式卡片时，后端调用 LLM、生成音频、写入 Anki
- 生成时后端检测 Anki/AnkiConnect 是否可用；若不可用，尝试自动启动 Anki

### 2.2 第一版非目标

第一版明确不做：

- 短语卡
- 扩展端直接调用 LLM
- 扩展端直接操作 Anki
- 观看时立即生成正式卡
- 自动截图
- 自动图片生成
- 多语言支持
- 多牌组 UI 配置
- 复杂设置中心
- YouTube 专用复杂字幕解析面板

---

## 3. 模块边界

### 3.1 浏览器扩展职责

浏览器扩展只负责以下事项：

1. 获取当前用户选中的单词
2. 尝试获取少量上下文
   - `surface_form`
   - `source_sentence`（可选）
   - `source_title`
   - `source_url`
   - `source_type`
   - `source_timestamp`（可选）
3. 将采集结果发送给本地后端
4. 根据后端返回结果显示轻提示

浏览器扩展禁止负责以下事项：

- 调用 LLM
- 生成音频
- 词形归并
- 队列去重
- Anki 去重
- 直接写入 Anki
- 自动启动 Anki
- 生成 HTML 卡片内容

### 3.2 本地后端职责

本地后端负责以下事项：

1. 输入标准化
2. 词形归并
3. 队列去重
4. Anki 查重
5. 待处理队列管理
6. 批量生成正式卡片
7. 调用 LLM（使用统一 `SYSTEM_PROMPT`）
8. 生成音频
9. 写入自定义 note type 字段
10. 依赖 Anki 模板渲染卡片
11. 失败记录与失败项单独重试
12. 生成版本号管理
13. 批量生成时检查 / 自动启动 Anki

---

## 4. 默认配置

第一版只保留最小配置能力，不做 UI 配置页。

### 4.1 默认配置项

- `default_deck_name = "sherber"`
- `default_note_type_name = "SherberVocabNote"`
- `default_generator_version = "v1"`

### 4.2 配置约束

- 默认牌组名必须来自统一配置
- 默认 note type 名称必须来自统一配置
- 不允许在扩展或后端业务逻辑中散落硬编码 `"sherber"`
- 不允许在扩展或后端业务逻辑中散落硬编码 `"SherberVocabNote"`
- 第一版不做多牌组切换功能
- 将来如需切换牌组，只允许通过集中配置改动

---

## 5. 输入与采集规则

### 5.1 输入对象

第一版只支持：

- 单个英文单词

第一版不支持：

- 短语
- 整句
- 多个单词组合
- 含复杂符号的字符串

### 5.2 标准化规则

后端收到扩展传来的原始输入后，必须执行标准化：

1. 去除首尾空白
2. 去除首尾标点
3. 统一小写
4. 校验是否合法英文单词
5. 非法输入直接拒绝

### 5.3 词形相关规则

必须区分以下四个概念：

- `surface_form`：用户在网页中实际选中的原始词形  
  例如：`retried`
- `lemma`：词形归并后的基础词形  
  例如：`retry`
- `word_key`：用于系统查重的单词业务键  
  第一版默认使用归并后的基础词形
- `record_id`：用于标识一条采集/队列记录的记录主键  
  与 `word_key` 不是同一个概念

### 5.4 去重规则

后端采集时按以下顺序处理：

1. 标准化输入
2. 词形归并
3. 生成 `word_key`
4. 查询待处理队列
5. 查询 Anki 中是否已有对应 `word_key`
6. 若重复，则不新建待处理项

---

## 6. 扩展 -> 后端 请求契约

### 6.1 请求目的

扩展向后端发送“采集一个待处理词项”的请求。

### 6.2 请求字段定义

| 字段名             | 中文含义   | 必填 | 来源           | 备注                       |
| ------------------ | ---------- | ---: | -------------- | -------------------------- |
| `surface_form`     | 原始词形   |   是 | 扩展采集       | 用户实际选中的文本         |
| `source_sentence`  | 来源原句   |   否 | 扩展采集       | 能获取则传，不能获取则为空 |
| `source_title`     | 来源标题   |   否 | 扩展采集       | 页面标题或视频标题         |
| `source_url`       | 来源链接   |   否 | 扩展采集       | 页面 URL                   |
| `source_type`      | 来源类型   |   是 | 扩展采集       | 枚举：`web` / `youtube`    |
| `source_timestamp` | 来源时间点 |   否 | 扩展采集       | 主要用于 YouTube           |
| `captured_at`      | 采集时间   |   否 | 扩展或后端补全 | 若扩展不传，后端生成       |

### 6.3 请求约束

- `surface_form` 为唯一必需核心输入
- 扩展只传原始采集数据，不传 `lemma`、`word_key`、`record_id`
- `lemma`、`word_key`、`record_id` 必须由后端生成
- 若 `source_sentence` 获取失败，不得阻塞采集流程

---

## 7. 后端 -> 扩展 响应契约

扩展不自行推断业务逻辑，只根据后端返回的标准状态展示提示。

### 7.1 状态枚举

| 状态值                | 中文含义         | 扩展提示建议        |
| --------------------- | ---------------- | ------------------- |
| `added_to_queue`      | 已加入待处理队列 | 已加入待处理        |
| `already_in_queue`    | 已在待处理队列中 | 已在待处理队列中    |
| `already_in_anki`     | 已存在于 Anki    | 该单词已存在于 Anki |
| `invalid_input`       | 输入不合法       | 当前仅支持单词      |
| `backend_unavailable` | 后端不可用       | 本地后端不可用      |
| `processing_failed`   | 处理失败         | 采集失败            |

### 7.2 响应字段建议

| 字段名      | 中文含义   | 必填 | 备注             |
| ----------- | ---------- | ---: | ---------------- |
| `status`    | 状态值     |   是 | 必须来自固定枚举 |
| `message`   | 文本说明   |   否 | 供调试或日志使用 |
| `record_id` | 记录主键   |   否 | 便于调试         |
| `word_key`  | 单词业务键 |   否 | 便于调试         |
| `lemma`     | 基础词形   |   否 | 便于调试         |

---

## 8. 待处理队列模型

待处理队列用于承接观看阶段的快速采集，避免即时重制卡打断体验。

### 8.1 队列项字段

| 字段名              | 中文含义   | 展示层级        | 备注                             |
| ------------------- | ---------- | --------------- | -------------------------------- |
| `record_id`         | 记录主键   | 不展示          | 队列记录唯一 ID                  |
| `surface_form`      | 原始词形   | 不展示          | 实际选中的词形                   |
| `normalized_form`   | 标准化词形 | 不展示          | 标准化后结果                     |
| `lemma`             | 基础词形   | 不展示          | 词形归并结果                     |
| `word_key`          | 单词业务键 | 不展示          | 查重主键                         |
| `source_sentence`   | 来源原句   | 不展示/中间字段 | 用于构造 `examples[0]`           |
| `source_title`      | 来源标题   | 不展示          | 后续队列面板可预留               |
| `source_url`        | 来源链接   | 不展示          | 来源追踪                         |
| `source_type`       | 来源类型   | 不展示          | `web` / `youtube`                |
| `source_timestamp`  | 来源时间点 | 不展示          | YouTube 场景使用                 |
| `captured_at`       | 采集时间   | 不展示          | 创建时间                         |
| `status`            | 队列状态   | 不展示          | `pending` / `success` / `failed` |
| `error_message`     | 错误信息   | 不展示          | 失败时保留                       |
| `generator_version` | 生成版本号 | 不展示          | 用于追踪生成逻辑版本             |

### 8.2 队列状态

- `pending`：待生成
- `success`：已生成正式卡片
- `failed`：生成失败，可重试

### 8.3 队列行为规则

- 观看阶段只加入 `pending`
- 生成正式卡片成功后更新为 `success`
- 生成失败时更新为 `failed`
- 失败项必须允许单独重试

---

## 9. Anki 自定义 Note Type 规格

### 9.1 设计原则

- 使用自定义 note type
- 使用字段化存储数据
- 使用 Anki 模板渲染卡片
- 不再依赖 `Basic` 的 Front/Back 承载全部信息

### 9.2 字段分层

字段必须分为三类：

1. 主展示字段
2. 弱展示字段
3. 不展示字段

---

## 10. 最终字段清单

### 10.1 主展示字段

| 字段名         | 中文含义 | 是否展示 | 说明         |
| -------------- | -------- | -------: | ------------ |
| `word`         | 单词     |       是 | 正面核心展示 |
| `ipa`          | 音标     |       是 | 正面展示     |
| `emoji`        | 记忆图标 |       是 | 正面展示     |
| `audio`        | 音频     |       是 | 正面播放     |
| `image_prompt` | 联想提示 |       是 | 正面弱展示   |
| `meanings`     | 英文释义 |       是 | 背面主展示   |
| `pairs`        | 固定搭配 |       是 | 背面主展示   |
| `examples`     | 例句     |       是 | 背面主展示   |

### 10.2 弱展示字段

| 字段名  | 中文含义 |     是否展示 | 说明                      |
| ------- | -------- | -----------: | ------------------------- |
| `forms` | 词形变化 | 是（弱显示） | 放在 Meaning 和 Pair 之间 |

### 10.3 不展示字段

| 字段名              | 中文含义   | 是否展示 | 说明                   |
| ------------------- | ---------- | -------: | ---------------------- |
| `record_id`         | 记录主键   |       否 | 队列/日志/重试定位     |
| `word_key`          | 单词业务键 |       否 | 系统查重主键           |
| `lemma`             | 基础词形   |       否 | 词形归并用             |
| `surface_form`      | 原始词形   |       否 | 保留原始采集输入       |
| `source_url`        | 来源链接   |       否 | 来源追踪               |
| `source_type`       | 来源类型   |       否 | 区分 `web` / `youtube` |
| `source_timestamp`  | 来源时间点 |       否 | 视频场景定位           |
| `generator_version` | 生成版本号 |       否 | 版本追踪               |

### 10.4 中间处理字段

| 字段名            | 中文含义 | 是否直接展示 | 说明                               |
| ----------------- | -------- | -----------: | ---------------------------------- |
| `source_sentence` | 来源原句 |           否 | 不单独展示，用于构造 `examples[0]` |

---

## 11. 卡片渲染规则

### 11.1 正面渲染规则

正面保留当前满意的结构：

1. `emoji`
2. `word`
3. `audio`
4. `ipa`
5. `image_prompt`

### 11.2 背面渲染规则

背面保留三大主区块：

1. `Meaning`
2. `Pair`
3. `Example`

并新增一个弱显示区块：

- `Forms`

### 11.3 `forms` 渲染规则

- 放置位置：`Meaning` 与 `Pair` 之间
- 展示强度：弱显示
- 有内容时才显示
- 不单独做大标题区块
- 推荐展示格式：

`Forms: retries / retried / retrying`

### 11.4 `examples` 渲染规则

#### 有 `source_sentence` 时
- `examples[0]` = 来源原句，并标注 `source sentence`
- `examples[1]` = 模型生成例句
- `examples[2]` = 模型生成例句

#### 无 `source_sentence` 时
- 三条都由模型生成

---

## 12. LLM 输出要求

后端必须基于统一 `SYSTEM_PROMPT` 请求模型生成结构化结果。

### 12.1 输出必须支持以下展示字段

- `word`
- `ipa`
- `emoji`
- `image_prompt`
- `meanings`
- `pairs`
- `examples`

### 12.2 后端补充字段

以下字段不应由扩展生成：

- `lemma`
- `word_key`
- `record_id`
- `forms`
- `generator_version`

其中：
- `forms` 可由后端根据词形规则生成
- `generator_version` 必须由后端统一写入
- `record_id` 必须由后端生成

---

## 13. word_key 生成规则

### 13.1 定义

`word_key` 是单词级别查重使用的业务键，不是记录主键。

### 13.2 第一版规则

第一版固定为：

> `word_key = lemma 的小写标准化结果`

示例：

- `Retry` -> `lemma = retry` -> `word_key = retry`
- `retried` -> `lemma = retry` -> `word_key = retry`
- `retrying` -> `lemma = retry` -> `word_key = retry`

### 13.3 约束

- `word_key` 必须可读，不使用随机字符串
- `word_key` 不使用 `id:12333` 这类记录编号
- `word_key` 仅用于单词去重和归并判断
- `record_id` 与 `word_key` 必须分离

---

## 14. Anki 连接规则

### 14.1 观看阶段

观看阶段只加入待处理队列，不依赖 Anki 是否打开。

### 14.2 批量生成阶段

批量生成正式卡片时：

1. 检查 AnkiConnect 是否可用
2. 若不可用，后端尝试自动启动 Anki
3. 启动成功后继续生成
4. 启动失败后返回明确错误

### 14.3 约束

- 自动启动 Anki 的逻辑必须由后端负责
- 扩展不负责启动本地程序

---

## 15. 命名与一致性约束

为避免扩展、后端、Anki 模板三方不一致，必须遵守：

1. 所有字段名以本 `SPEC.md` 为准
2. 扩展不得自定义后端字段名
3. 后端不得随意新增未记录字段
4. note type 字段名必须与本文件保持一致
5. 状态枚举必须与本文件保持一致
6. 修改字段前必须先修改 `SPEC.md`

---

## 16. 开发流程约束（Codex 使用规则）

Codex 实现代码前，必须先遵守本文件，不得自行更改以下内容：

- 模块边界
- 字段命名
- 状态枚举
- `forms` 显示位置
- `examples` 规则
- 默认 deck 名称来源方式
- `default_note_type_name`
- `word_key` 生成规则
- `record_id` 与 `word_key` 的职责分离

若实现中发现规格不合理，必须先修改 `SPEC.md`，再改代码。

---

## 17. 当前已确认决策

以下内容已经确认，不得在实现阶段随意更改：

- 浏览器扩展只做轻量选词采集
- 本地后端负责全部重逻辑
- 观看阶段不生成正式卡片
- 观看阶段不依赖 Anki 打开
- 批量生成阶段才连接 Anki
- 若 Anki 未打开，由后端尝试自动启动
- 第一版只支持单词，不支持短语
- 使用自定义 note type
- 自定义 note type 名称为 `SherberVocabNote`
- 使用字段化存储 + Anki 模板渲染
- `forms` 为弱展示字段
- `forms` 放在 Meaning 和 Pair 之间
- `source_sentence` 不单独展示，用于构造 `examples[0]`
- 默认 deck 名为 `sherber`
- `word_key = lemma 的小写标准化结果`
- `record_id` 为记录主键，不能替代 `word_key`